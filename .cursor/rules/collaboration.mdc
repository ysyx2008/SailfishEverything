---
description: 人机协作开发规范，定义 AI Agent 在开发过程中的行为要求
alwaysApply: true
---

# 协作开发规范

用户负责：需求定义、架构决策、验收测试。AI 负责：编码、测试、代码审查。

## 本机工具

- **GitHub CLI (`gh`)**：可使用。

## 开发流程

1. **点子先入想法本**：对话里冒出、当时来不及做的点子，记进 `docs/ideas.md`，不因此开工。想法本走「想法 → 方案 → 已落实」；大改动的完整方案仍按下面第 3 条另落独立 plan
2. **先理解再动手**：改动前充分阅读相关代码上下文（含对应 SPEC 的设计目标）
3. **大改动先出方案**：涉及 3 个以上文件的改动，先说明方案（改哪些文件、怎么改、影响什么），用户确认后再编码。大改动的方案落盘为 `docs/plans/YYYY-MM-DD-<主题>-design.md`（plan 文档，实现细节的合法住所：类名/接口/数据模型随意用）；**文件名和文首都必须写落盘日期（`YYYY-MM-DD`）**；未完成不加前缀、文首写「未完成」，做完改成 `done-YYYY-MM-DD-<主题>-design.md` 且文首改成「已完成」——按文件名排序时做完的挤在 `done-` 一堆，没做的按日期排在后面，要关注的一眼能找；小改动对话确认即可，不必落盘
4. **设计先落 SPEC 再编码**：讨论中用户提出并确认的设计思路（目标、取舍、不做的事）必须先写入对应 SPEC 的「设计目标」，**然后才动手改代码**。对话里的共识不算数，SPEC 里的才算数
5. **不确定就问**：需求有歧义或有多种实现方式时，先问清楚，不要猜

## 改动完成后必须做

1. **跑测试**：至少跑 `bash scripts/test.sh`；能端到端就端到端
2. **能端到端就端到端，别全押在回归和单测上**：回归保的是「原来能用的没坏」，单测保的是「在我理解的模型下实现是对的」——可 bug 恰恰常出在**我的理解本身就错了**，那种错单测天生测不出来，只有让真东西跑起来才露头。跑出来别只看断言绿不绿，日志里的警告、对不上的数，往往就是「模型与现实不符」露的头。
3. **变更摘要**：改动完成后给用户简明总结 — 改了哪些文件、做了什么、有什么需要注意的
4. **想法本**：做完就打开 `docs/ideas.md`，对得上的点子挪到「已完成」；详见 `ideas.mdc`

## 大功能额外要求

- 拆成可独立验证的小步；大 plan 在文档中先列「任务拆解」（原子任务 + 每步验收方式）
- 重构和新功能分开做，不要混在一起
- **plan 文档完成时两个动作**：① 讨论确认的稳定取舍升华进对应 SPEC 设计目标（转译为自然语言，去掉实现名）；② **标明已完成**——文件名改成 `done-YYYY-MM-DD-…`，文首改成「已完成」，任务拆解勾掉已做的。**不要删**：目录里不带 `done-` 的才是还能接着翻、决定要不要继续的；删了就只剩一堆不知死活的破烂。暂缓也算未完成（文首写「未完成（暂缓）」，文件名不加 `done-`）

## SPEC 文档要求

- **SPEC 记录用户的设计要求，不是实现索引**：SPEC 只记录讨论中确认的功能要求、设计取舍、行为承诺；禁止出现类名/方法名/API 表等代码实现内容（检验标准：每句话都应是用户能说出口、能看懂、能验收的话）。详见 `spec-driven.mdc`
- **改模块前先读 SPEC.md**：改动任何模块前，必须先读取对应的 `SPEC.md`（或 `*_SPEC.md`）。若不存在，先写设计目标再动手改代码
- **SPEC 只在讨论确认新设计决策时更新**：没有「改代码就要同步 SPEC」的义务；查代码结构看代码本身，不靠 SPEC
- **新模块必须带 SPEC**：完成新模块后必须有对应 SPEC.md（含设计目标）

## 禁止事项

- 不跑测试就说「完成了」
- 未经确认进行大范围重构
- 写基于关键词匹配的脆弱代码
- **给一个概念造第二个真相源**：多一处定义就多一个会各自演化的真相，日后必然对不上。抄一遍是，换个词重新发明也是。只有真正的新概念才配新建类型。
