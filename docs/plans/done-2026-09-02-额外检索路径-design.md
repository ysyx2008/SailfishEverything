# 已完成

2026-09-02 额外检索路径（含默认纳入微信聊天文件）

## 要解决的问题

容器整段排除，微信聊天里传来的文档进不了名单。这些文档在微信自己的「聊天文件」目录，文件名是人话，值得搜。用户还希望：排除名单之外，能自己加要搜的路径。

## 取舍

- 不新开第三张表。设置里已有「也索引这些文件夹」，现在只认家目录外面的盘；把它扩成真正的纳入名单。
- 排除照旧：容器、缓存、云盘默认仍跳过。
- 纳入可以打穿排除：路径就算落在已排除的父目录下，也单独扫这一枝。
- 已经在家目录扫描范围内的路径，加进纳入名单也不扫第二遍。
- 微信聊天文件作为默认纳入项，设置里能去掉。不写死某个账号的完整路径（账号目录名因人而异）；扫的时候只认「微信容器里各账号下的聊天文件目录」。
- 只要文档那一层，不要图片/视频/备份。
- 不按文件名像不像人话来筛。

## 改哪些

- 名单设置与扫盘（核心）：纳入名单怎么存、默认微信项怎么解析、扫的时候多走这些根
- 设置窗口：纳入列表展示默认项和用户自加项；去掉纳入项与去掉排除项对称
- 文案：说明「排除的仍跳过，但下面这些会单独算上」
- SPEC：核心（范围）+ 窗口（设置能做什么）
- 测试：排除下的纳入能搜到；范围内的纳入不重复；去掉默认微信后搜不到；外置盘仍可用

## 任务拆解

- [x] SPEC 写入已确认取舍
- [x] 纳入名单：默认微信 + 用户自加；家目录内已覆盖的不重复扫
- [x] 扫盘时单独走纳入根（不受父级排除挡住）
- [x] 设置列表能看见、能去掉默认微信、能加任意文件夹
- [x] 测试与真实扫一遍微信文档目录

## 明确不做

- 不默认放开整片容器
- 不搜聊天正文
- 不把微信图片/视频缓存算进来
