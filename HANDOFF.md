# Sailfish Everything — 交接

日期：2026-08-31  
状态：还没开工。先判断「能不能干」，不是开始做产品。  
这不是旗鱼功能，是另做的独立软件。下面只交接对话里已经对齐的事。

---

## 要解决什么

从 Windows 用惯了 Everything，换到 Mac 之后，找不到同等手感的东西。

日常用法很明确：**记得文件名里有几个字**，想立刻摊开所有对得上的文件，靠路径自己认。不是按正文内容找，也不要 Spotlight 那种「猜你想干什么」。

痛点是两件叠在一起：

1. **搜的范围不对**：Spotlight 把正文、邮件、网页、词典跟文件名混在一起排，名字完全对上的反而被挤下去。
2. **看的方式不对**：结果窗只有一条缝，五六条就没了。Everything 是一张大表，路径一眼能辨。

Spotlight 没有「只搜文件名」的总开关。`name:会议纪要`、Finder 里把条件改成「名称」能救急，但那是一次查询，不是 Everything 的肌肉记忆。

---

## 产品是什么 / 不是什么

**是**：Mac 上的 Everything。只认文件名（和路径）。常驻、边敲边出、结果铺满。

**不是**：启动器（Alfred / Raycast）、全文搜索、Spotlight 套皮、旗鱼的一个功能。

对手不是 Spotlight，是「有没有人已经把这口饭吃成习惯」。目前还没有。voidtools 也没出过 Mac 版。

已经看过、对不上手感的：

| 东西 | 为什么不像 |
|---|---|
| Spotlight / Finder 搜索 | 全文乱入；窗口是一条缝 |
| Alfred / Raycast | 启动器附属，列表太短 |
| EasyFind / Find Any File | 能按文件名搜，但是开窗再扫，不是边敲边出 |
| **Mverything**（uTools 插件，已实测） | 必须按回车才搜。作者自己写了：底层是 `mdfind`，做不到实时就放弃了 |

Mverything 这一下测到的，是仿制品最常见的走样：**皮像，手感不像。**

---

## 不能让的一条

**边敲边出。** 每多一个字，列表立刻变窄。回车把「边想边找」变成「先猜完再问」，手感断了。

没这个，就不必做。

---

## 技术结论：能做

Everything 快，快在名单已经在手里。Windows 上它读 NTFS 总账 + 变更日志，文件名躺在内存里。每敲一个字只是筛这份名单。几百万条短字符串，现在的 CPU 几十毫秒内能扫完；下一个字只在上一轮剩下来的结果里继续收窄。回车没有任何技术必要。

Mac 缺的是那本公开总账（APFS 不给你同等接口），**不是筛选本身**。

做得到的做法：

1. 自己扫一遍盘，养一份**只含路径和文件名**的名单。
2. 之后用系统的文件变动通知往上补。
3. 敲字只在这份名单里筛。不碰正文，不跑 Spotlight，不每次开进程去查。

量级撑得住：普通 Mac 一两百万到几百万个文件，只记路径，内存通常几十到一两百兆。「只搜标题」反而能做成即时的。

Mverything 必须回车，是架构选错了：每次去问 Spotlight。那是「每次出门问别人」，不是「名单在自己桌上翻」。**做不到的是「零成本复用系统索引还要即时」**，那条路已经证伪。自己养名单，敲字只筛名单——这正是 Everything 的那一下。

跟 Windows 真正的差别在**养名单**，不在搜：

- 第一次扫描要等一会儿（可以边建边搜）
- 变动通知有时会丢，需要偶尔对一遍
- 要「完全磁盘访问」才能看见全盘
- iCloud、外置盘、系统保护目录是脏活

这些影响「全不全、新不新」，不妨碍敲第三个字时列表已经变了。

---

## 下一聊建议先拍板的

新仓库里接着聊，建议先把「能不能干」收成可验收的一两件事，再谈产品外壳：

1. **本机刺探**：扫一遍家目录（或可勾选的范围），把文件名名单养起来，敲字即时筛，结果铺开带路径。不接 Spotlight。用来亲眼确认手感，不是做完产品。
2. **范围**：第一刀只做本机已下载的文件。iCloud 占位、外置盘、网络盘可以后说。
3. **平台**：先 Mac。Windows 已有 Everything，不必抢。

还没定、先别写进设计的：收费、上架、中文分词/拼音、正则、Everything 那套 `ext:` 语法、预览、当启动器用。

---

## 对话出处

2026-08-31 在旗鱼仓库里纯讨论，明确不记旗鱼想法本。用户自己建新仓库后，把这份文档拷进去继续。
